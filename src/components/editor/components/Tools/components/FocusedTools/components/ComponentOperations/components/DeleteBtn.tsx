import { Button } from "zui-pro";
import { DeleteOutline } from "@zui-pro/icons";

export const DeleteBtn = () => {
  return <Button size="small" type="text" icon={<DeleteOutline size={16} />} />;
};
